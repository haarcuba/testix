this command's syntax is

`/style [FILE1] [FILE2] ...`

- if no files are specified, do it for recently modified files
- here are the style rules to follow
    - always use `import * as module from 'path/to/module'` and not `import { something } from 'path/to/module'`
